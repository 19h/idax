/// \file helpers.cpp
/// \brief Shared helper implementations.

#include "helpers.hpp"

// Intentionally minimal — most helpers are inline in the header.
// This TU exists to ensure the header compiles and to anchor any
// future non-inline helper implementations.
